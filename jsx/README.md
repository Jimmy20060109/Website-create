# ⚛️ JSX — Minimal React + Vite Starter

A tiny project to practice JSX, inline styles, and the basic React render flow. Renders a simple input with a blue border. ✨

## ✨ Features
- JSX component with inline style object
- `createRoot` render flow with clear steps
- Clean starter setup for quick experiments

## 🚀 Quick Start
```bash
npm install
npm start
```
Open the dev URL shown in the terminal (default `http://localhost:5173`).

## 🗂️ Structure
- `src/App.js` — returns an `<input />` with inline styles
- `src/index.js` — imports React, gets `#root`, calls `createRoot`, renders `<App />`
- `index.html` — contains the `#root` mount point

## 🛠️ Customize
- Tweak the style object in `App.js`
- Replace `<input />` with any JSX to experiment
- Add state/events to explore React basics 🧪
